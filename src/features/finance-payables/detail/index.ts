// Step 15B: the Payable detail page (/finance/payables/[payableRef]). The server page resolves the
// actor, reads the Payable and the source-revision check, computes permissions, then renders this
// client component.
export { PayableDetail } from "./PayableDetail";
export { parseDetailTab } from "./detail-view";
