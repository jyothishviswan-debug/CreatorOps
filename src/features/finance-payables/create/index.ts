// Step 15B: the Create Payable flow (/finance/payables/new). The server page resolves the actor and
// computes permissions, then renders this client page.
export { PayableCreatePage } from "./PayableCreatePage";
