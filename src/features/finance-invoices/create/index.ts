// Step 16B: the Create Invoice flow (/finance/invoices/new). The server page
// (src/app/finance/invoices/new/page.tsx) resolves the actor, computes permissions and renders this
// client component.
export { InvoiceCreatePage } from "./InvoiceCreatePage";
