// Step 17B: the Record Payment flow (/finance/payments/new). The server page
// (src/app/finance/payments/new/page.tsx) resolves the actor and permissions and hands them to
// this client shell.
export { PaymentCreatePage } from "./PaymentCreatePage";
